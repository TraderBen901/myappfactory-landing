import { landingPage } from './landingPage';
import { siteSettings } from './siteSettings';
import { appCard, card, link, processStep, richText, stat, trainingLevel } from './objects';

export const schemaTypes = [
  siteSettings,
  landingPage,
  link,
  stat,
  card,
  appCard,
  processStep,
  trainingLevel,
  richText,
];
